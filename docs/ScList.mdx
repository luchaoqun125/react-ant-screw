---
name: ScList
route: /ScList
order: 3
sidebar: true
---

import { ScList } from '../src';
import { formConfig } from '../screwConfig/form.config.js'
import { tableConfig } from '../screwConfig/table.config.js'
import 'antd/dist/antd.css';
formConfig.props.layout = 'inline'
const options = {
  formConfig,
  tableConfig,
  onMount: (list) => { console.log(list) }
}

# ScList Component

<ScList {...options}></ScList>